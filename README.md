# TODO: Add Proper Readme

Feature : Uses local sqlite so i can be backuped and encripted and synced
uses hash instead of password for groundbreaking sequrity
uses sqlite in user dirctory so that any application can use it

Feature : Can create new users with diffrent roles
role : vehicleops, client, safety officer, admin
and creation of roles is enforced by the backend so it hardens the security